# 测试说明

## 1. 测试目标

XBuddy 是一个前端可交互 Demo。测试重点不是校验金融预测正确性，而是验证用户能否完成一次有监督的研究流程，并确认异常、数据缺失与合规边界不会被产品静默掩盖。

本说明覆盖：

- 从对话输入研究目标，到计划、执行、人工批准、最终研究结果的主链路；
- 任务面板、长期记忆、证据链、产物等可操作入口；
- 数据源超时、备用源审批、暂停与恢复等异常处理；
- 不将模拟研究误导为投资建议的合规边界；
- 构建与静态站点发布的工程回归。

## 2. 测试环境与当前结果

| 项目 | 配置 / 方法 | 结果 |
| --- | --- | --- |
| 运行时 | Node.js + Vite + React | 用于本地开发和生产构建 |
| 生产构建 | `npm run build` | 通过 |
| 发布结构检查 | `npm run test:sites` | 通过，4/4 通过 |
| 公共环境 | GitHub Pages | 已部署至 [公开 URL](https://qy1018.github.io/x-buddy-agent-workbench/) |
| 浏览器视觉回归 | 内置浏览器导航本地服务 | 受 `net::ERR_BLOCKED_BY_CLIENT` 阻断，未标记通过 |

### 执行命令

```bash
npm install
npm run build
npm run test:sites
npm run dev
```

最后一项用于在允许访问本地地址的浏览器中执行下列手工交互回归。若部署到 GitHub Pages，访问公开 URL 即可执行同样的场景。

## 3. 主链路测试

| ID | 场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| F-01 | 输入研究目标 | 在欢迎页对话框输入“验证半导体库存周期是否触底”，点击发送 | 创建研究线程，目标出现在会话中，右侧任务面板显示计划草案 |
| F-02 | 新建研究入口 | 点击左侧“新建研究”或研究列表中的“新建项目” | 返回研究目标输入状态；临时进度清空，不覆盖长期记忆 |
| F-03 | 计划确认 | 在计划草案下点击确认 | 进入执行状态；研究范围、工具、预算与首项任务可见 |
| F-04 | 首轮采集 | 点击“执行采集” | 出现公司披露和行业月度数据两条原始证据，任务进度和工具状态更新 |
| F-05 | 交叉验证 | 点击“执行验证” | 进入验证阶段，记录参与验证的工具与当前产物 |
| F-06 | 故障暴露 | 等待 / 触发 OTIX 超时阶段 | 明确显示超时原因、失败记录与备用来源；不得直接生成结论 |
| F-07 | 人工批准备用源 | 点击“批准备用源” | 记录确认动作，进入检查点/继续执行路径；右栏保留失败与备用源信息 |
| F-08 | 保存并暂停 | 在故障检查点点击“保存并暂停” | 状态变为暂停，保留上下文、计划与已采集证据，便于后续恢复 |
| F-09 | 最终审批 | 在结论生成前的检查点批准 | 仅在批准后转入 `memo` 状态并生成最终研究稿 |
| F-10 | 结果直接阅读 | 任务完成后查看主区域 | 直接展示结论、事实/推断/不确定性、趋势图、研究正文和证据链，不以文件卡片代替正文 |
| F-11 | 证据链交互 | 点击“查看证据链” | 展开/收起 4 条证据，能看到来源、时间、字段和用途说明 |
| F-12 | 右栏收起 | 点击任务面板收起按钮，再点击侧边标签 | 面板可展开/收起，主线程宽度恢复，研究状态不丢失 |
| F-13 | 长期记忆 | 点击“长期记忆”标签，再选择任一条目 | 显示研究方法、来源偏好和待验证假设；选择动作写入当前研究提示 |

### 主链路验收标准

- F-01 至 F-13 均可完成，且没有刷新页面或重新打开页面的必要；
- 在 F-09 之前，页面不能显示“最终研究结果”；
- 所有阶段均能从右侧面板看到对应任务、工具或产物变化；
- F-06 的失败记录不可被隐藏或伪装成成功；
- F-10 的结论必须包含信息分层和来源回溯入口。

## 4. 数据缺失、接口失败与恢复测试

| ID | 输入 / 条件 | 预期行为 | 不可接受行为 |
| --- | --- | --- | --- |
| E-01 | OTIX 渠道库存接口超时 | 显示超时、影响范围、备用来源和审批按钮 | 自动跳过失败并把交叉验证标为完成 |
| E-02 | 用户不批准备用源 | 停留在检查点或保存为暂停状态 | 无用户操作时自动发布结论 |
| E-03 | 用户批准 TechInsights 备用源 | 在运行记录中保留原始失败与备用选择 | 覆盖或删除 OTIX 失败痕迹 |
| E-04 | 上下文中存在未证实假设 | 将其标记为“待验证”，不并入已验证事实 | 作为确定性研究结论陈述 |
| E-05 | 研究范围过宽或含混 | 计划阶段显示范围、验证目标和确认动作 | 不经确认直接宣称完成深度研究 |
| E-06 | 用户点击暂停 | 保留计划、已有证据、上下文和下一步动作 | 清空状态或生成半成品最终报告 |

### 恢复逻辑的产品意义

真实研究中的失败并不等于“没有发生”。因此 Demo 将失败保留为可见的运行事件，并将是否启用备用源交回给用户。这既体现 Agent Harness 的可恢复机制，也避免因来源变化导致研究质量、成本或许可条件在用户不知情时被改变。

## 5. 极端与合规边界测试

| ID | 边界场景 | 检查点 | 预期结果 |
| --- | --- | --- | --- |
| C-01 | 用户把研究目标表述为“推荐最值得买的股票” | 目标输入与结果文案 | 产品以研究/验证框架回应，不输出个性化买卖指令 |
| C-02 | 数据出现单一来源、短期异常波动 | 事实与推断区 | 显示为证据或待验证信号，不升级为确定结论 |
| C-03 | 结论涉及周期位置 | 标题、正文与不确定性标签 | 使用“早期信号”“尚不充分”等审慎措辞，并保留风险项 |
| C-04 | 页面显示模型成本、工具名称或数据来源 | 任务面板与证据链 | 明确为演示性工具/数据，不暗示真实金融终端接入 |
| C-05 | 用户未通过最终检查点 | 状态机门控 | 不显示最终研究报告、趋势图或归档产物 |
| C-06 | 长期记忆包含方法偏好 | 长期记忆区域 | 展示为研究偏好/假设，不作为自动交易规则 |
| C-07 | 任何收益率、价格或胜率描述 | 全页文案扫描 | 不承诺收益，不给出确定价格预测 |
| C-08 | 数据来源许可不明确 | 真实接入的设计边界 | 当前仅模拟；未来必须存储授权、许可与字段血缘 |
| C-09 | 用户需要审计“为何得出结论” | 证据链与过程记录 | 可回溯来源、时间、字段和研究用途 |

## 6. 发布前回归清单

- [x] `npm run build` 通过。
- [x] `npm run test:sites` 通过（4/4）。
- [x] Vite 在 GitHub Actions 下使用 `/x-buddy-agent-workbench/` 基路径。
- [x] GitHub Pages 工作流存在并从 `main` 发布。
- [x] README、AI 使用与验证记录、测试说明包含真实边界与未做事项。
- [ ] 在可访问页面的浏览器中执行 F-01 至 F-13 的截图级交互回归。
- [ ] 在窄桌面与移动宽度下完成响应式和键盘可达性检查。
- [ ] 真实金融数据接入后，对来源许可、时间戳、字段血缘和降级策略进行端到端测试。

## 7. 已知测试限制与后续计划

当前执行环境的内置浏览器拒绝本地服务导航，错误为 `net::ERR_BLOCKED_BY_CLIENT`。因此无法在本环境采集浏览器控制台、截图或逐项点击录屏。工程构建和静态发布结构已完成验证，但视觉与交互验收应在能够打开本地或公开 Pages 地址的浏览器中补做；详情见 `design-qa.md`。

若后续接入真实数据和后端 Agent，建议增加：工具调用契约测试、任务中断恢复测试、数据刷新一致性测试、访问控制测试、提示注入与恶意来源测试、审计日志完整性测试，以及针对投资建议/数据授权的人工合规评审。

